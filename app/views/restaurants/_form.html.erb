<%= form_for(restaurant) do |f| %>
  <% if restaurant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(restaurant.errors.count, "error") %> prohibited this restaurant from being saved:</h2>

      <ul>
      <% restaurant.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :address %>
    <%= f.text_field :address %>
  </div>

  <div class="field">
    <%= f.label :openings_time %>
    <%= f.time_select :openings_time %>
  </div>

  <div class="field">
    <%= f.label :closings_time %>
    <%= f.time_select :closings_time %>
  </div>

  <div class="field">
    <%= f.label :opening_day %>
    <%= f.text_field :opening_day %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
