%p#notice= notice
%h1= pluralize(@restaurants.count, "Restaurants")

%table.table.table-condensed
  %thead
    %tr
      %th Name
      %th Address
      %th Menu
      %th Openings time
      %th Closings time
      %th Opening day
      %th Closing day
      %th Actions
  %tbody
    = render @restaurants
%br/
= link_to 'New Restaurant', new_restaurant_path, class: 'btn btn-info'
